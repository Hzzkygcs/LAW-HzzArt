<template class="add-image-card-template">
    <div class="relative w-full rounded-xl">
        <a href="<%= urls.collections.addImagePage() %>">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-1/4 h-full mx-auto hover:stroke-blue-700">
                <path stroke-linecap="round" stroke-linejoin="round" d="M12 9v6m3-3H9m12 0a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
        </a>
    </div>
</template>

<script>
    function spawnAddImageCardBtn(parent) {
        const ret = $($(".add-image-card-template").html());
        if (parent != null)
            parent.append(ret);
        return ret;
    }
</script>